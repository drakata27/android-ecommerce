<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.VideoGamesStore" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your light theme here. -->
         <item name="colorPrimary">@color/blue</item>
    </style>

    <style name="Theme.VideoGamesStore" parent="Base.Theme.VideoGamesStore" />

    <!-- Theme without ActionBar -->
    <style name="NoActionBarTheme" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/blue</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="AppTheme.Button" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorButtonNormal">@color/blue</item>
    </style>

</resources>